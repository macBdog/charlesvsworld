node_modules/
.DS_Store
Thumbs.db
*.tmp

# Content — lives on the GCP host, not in git
content/

# Synced webapps — pulled from GitHub during deployment
webapps/

# Build output — generated during deployment, not in git
_build/
